mixin thumbnail-creative( obj )
	// Thumbnail Creative
	a.thumbnail-creative( href= obj.link )&attributes( attributes )
		+img( obj.img ).thumbnail-creative-image
		h5.thumbnail-creative-title= obj.title
